filter:
    excluded_paths:
        - 'tests/*'
        - "src/database/migrations/*"
        - "src/resources/*"
checks:
    php: true
coding_style:
    php:
      spaces:
        around_operators:
            concatenation: true
            negation: true